{
    "version": 2,
    "public": true,
    
    "builds": [{
        "buildCommand": "npm run compile",
        "src": "./dist/index.js",
        "use": "@vercel/node"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/dist/"
    }]
  }