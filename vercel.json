{
    "version": 2,
    "public": true,
    "buildCommand": "npm run compile",
    "builds": [{
        "src": "./dist/index.js",
        "use": "@vercel/node"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/dist/"
    }]
  }