{
    "version": 2,
    "headers": [{
        "key": "Access-Control-Allow-Origin", "value": "*"
    }],
    "builds": [{
        "src": "./dist/index.js",
        "use": "@vercel/node"
    }],
    "routes": [{
        "src": "./dist/(.*)",
        "dest": "/"
    }]
}